import { Badge } from '../../components/ui/Badge';
import { Button } from '../../components/ui/Button';
import type { AdminPayment } from '../../api/adminResources';
import { formatDateTime, formatMoney, paymentMethodLabel, paymentStatusLabel, paymentStatusTone } from './paymentUtils';
export function PaymentList({ payments, selectedId, onSelect }: { payments: AdminPayment[]; selectedId?: string; onSelect: (payment: AdminPayment) => void }) {
  return <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table className="min-w-full divide-y divide-slate-200 text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Paciente / cita</th><th className="px-4 py-3">Profesional</th><th className="px-4 py-3">Método</th><th className="px-4 py-3">Estado</th><th className="px-4 py-3">Monto</th><th className="px-4 py-3">Evidencias</th><th className="px-4 py-3">Acción</th></tr></thead><tbody className="divide-y divide-slate-100">{payments.map((p) => <tr key={p.id} className={selectedId === p.id ? 'bg-brand-50/60' : undefined}><td className="px-4 py-3"><div className="font-semibold text-slate-900">{p.patient_name ?? 'Paciente sin nombre'}</div><div className="text-xs text-slate-500">{formatDateTime(p.appointment_starts_at)} · {p.practitioner_service_name ?? 'Servicio'}</div><div className="text-xs text-slate-500">{[p.organization_name, p.location_name, p.room_name].filter(Boolean).join(' · ')}</div></td><td className="px-4 py-3">{p.practitioner_name ?? '—'}</td><td className="px-4 py-3">{paymentMethodLabel(p.method)}</td><td className="px-4 py-3"><Badge tone={paymentStatusTone(p.status)}>{paymentStatusLabel(p.status)}</Badge></td><td className="px-4 py-3 font-semibold">{formatMoney(p.amount, p.currency)}</td><td className="px-4 py-3">{p.evidence_count}</td><td className="px-4 py-3"><Button variant="secondary" onClick={() => onSelect(p)}>Ver detalle</Button></td></tr>)}</tbody></table></div>;
}
