import { useMemo, useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { useForm } from 'react-hook-form';
import { z } from 'zod';
import { zodResolver } from '@hookform/resolvers/zod';
import { PageHeader } from '../../components/layout/PageHeader';
import { Button } from '../../components/ui/Button';
import { SectionCard } from '../../components/ui/Card';
import { DataTableShell } from '../../components/ui/DataTable';
import { FieldWrapper, Input } from '../../components/ui/Form';
import { EmptyState, ErrorState, LoadingState } from '../../components/ui/States';
import { adminResourcesApi, type Practitioner, type PractitionerSpecialty, type Specialty } from '../../api/adminResources';
import type { AdminTenant } from '../../config/tenant';
import { NameEditButton, StatusAction, StatusBadge } from '../adminResourceUtils';
import { emptyToNull } from '../adminResourceFormat';
const schema = z.object({ full_name: z.string().min(1, 'El nombre completo es obligatorio'), professional_type: z.string().optional(), professional_license: z.string().optional(), email: z.string().email('Ingresa un correo válido').or(z.literal('')).optional(), phone: z.string().optional() });
type FormValues = z.infer<typeof schema>; const defaults = (p?: Practitioner): FormValues => ({ full_name: p?.full_name ?? '', professional_type: p?.professional_type ?? '', professional_license: p?.professional_license ?? '', email: p?.email ?? '', phone: p?.phone ?? '' });
function Chip({ label, inactive, onRemove }: { label: string; inactive?: boolean; onRemove: () => void }) { return <span className="inline-flex items-center gap-2 rounded-full border border-brand-100 bg-brand-50 px-3 py-1 text-sm font-medium text-brand-800">{label}{inactive ? <span className="text-xs text-amber-700">inactiva</span> : null}<button type="button" className="text-brand-700 hover:text-brand-950" onClick={onRemove}>×</button></span>; }
export function PractitionersPage({ tenant }: { tenant: AdminTenant }) { const [showForm, setShowForm] = useState(false); const [editing, setEditing] = useState<Practitioner | null>(null); const [feedback, setFeedback] = useState<string | null>(null); const [selected, setSelected] = useState<PractitionerSpecialty[]>([]); const [search, setSearch] = useState(''); const queryClient = useQueryClient(); const practitionersKey = useMemo(() => ['practitioners', tenant.id], [tenant.id]); const specialtiesKey = useMemo(() => ['specialties', tenant.id], [tenant.id]); const practitioners = useQuery({ queryKey: practitionersKey, queryFn: () => adminResourcesApi.listPractitioners(tenant.id) }); const specialties = useQuery({ queryKey: specialtiesKey, queryFn: () => adminResourcesApi.listSpecialties(tenant.id) }); const form = useForm<FormValues>({ resolver: zodResolver(schema), defaultValues: defaults() }); const payload = (v: FormValues) => ({ full_name: v.full_name.trim(), professional_type: emptyToNull(v.professional_type), professional_license: emptyToNull(v.professional_license), email: emptyToNull(v.email), phone: emptyToNull(v.phone) }); const closeForm = () => { setEditing(null); setShowForm(false); setSelected([]); setSearch(''); form.reset(defaults()); };
  const syncSpecialties = async (practitionerId: string, previous: PractitionerSpecialty[], next: PractitionerSpecialty[]) => { const prevActive = new Set(previous.filter((x) => x.status === 'active').map((x) => x.specialty_id)); const nextIds = new Set(next.map((x) => x.specialty_id)); for (const specialtyId of nextIds) if (!prevActive.has(specialtyId)) await adminResourcesApi.assignPractitionerSpecialty(tenant.id, practitionerId, specialtyId); for (const specialtyId of prevActive) if (!nextIds.has(specialtyId)) await adminResourcesApi.disablePractitionerSpecialty(tenant.id, practitionerId, specialtyId); };
  const save = useMutation({ mutationFn: async (v: FormValues) => { const previous = editing ? await adminResourcesApi.listPractitionerSpecialties(tenant.id, editing.id) : []; const saved = editing ? await adminResourcesApi.updatePractitioner(tenant.id, editing.id, payload(v)) : await adminResourcesApi.createPractitioner(tenant.id, payload(v)); await syncSpecialties(saved.id, previous, selected); return saved; }, onSuccess: async () => { setFeedback(editing ? 'Profesional actualizado correctamente.' : 'Profesional creado correctamente.'); closeForm(); await queryClient.invalidateQueries({ queryKey: practitionersKey }); } }); const statusAction = useMutation({ mutationFn: (p: Practitioner) => p.status === 'active' ? adminResourcesApi.disablePractitioner(tenant.id, p.id) : adminResourcesApi.activatePractitioner(tenant.id, p.id), onSuccess: async (_updated, p) => { setFeedback(p.status === 'active' ? 'Profesional inactivado correctamente.' : 'Profesional activado correctamente.'); await queryClient.invalidateQueries({ queryKey: practitionersKey }); } });
  const startEdit = async (p: Practitioner) => { setEditing(p); form.reset(defaults(p)); setShowForm(true); setSelected([]); try { setSelected((await adminResourcesApi.listPractitionerSpecialties(tenant.id, p.id)).filter((x) => x.status === 'active')); } catch { setFeedback('No se pudieron cargar las especialidades del profesional.'); } };
  const selectable = (specialties.data ?? []).filter((s: Specialty) => s.status === 'active' && !selected.some((x) => x.specialty_id === s.id) && s.name.toLowerCase().includes(search.toLowerCase()));
return <><PageHeader eyebrow="Fase 6B.4 · especialidades múltiples" title="Profesionales" description="Gestiona profesionales y sus especialidades mediante nombres legibles." actions={<Button onClick={() => showForm ? closeForm() : setShowForm(true)}>{showForm ? 'Cerrar formulario' : 'Crear profesional'}</Button>} />{feedback ? <div className="mb-4 rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm font-medium text-emerald-800">{feedback}</div> : null}{save.isError ? <div className="mb-4"><ErrorState description={(save.error as Error).message} /></div> : null}{statusAction.isError ? <div className="mb-4"><ErrorState description={(statusAction.error as Error).message} /></div> : null}{showForm ? <SectionCard title={editing ? `Editar ${editing.full_name}` : 'Nuevo profesional'} description="Registra datos básicos y asigna cero, una o múltiples especialidades."><form className="grid gap-4 md:grid-cols-2" onSubmit={form.handleSubmit((v) => save.mutate(v))}><FieldWrapper label="Nombre completo" error={form.formState.errors.full_name?.message}><Input placeholder="Dra. Ana Pérez" {...form.register('full_name')} /></FieldWrapper><FieldWrapper label="Tipo profesional" error={form.formState.errors.professional_type?.message}><Input placeholder="Médico general" {...form.register('professional_type')} /></FieldWrapper><FieldWrapper label="Registro / licencia profesional" error={form.formState.errors.professional_license?.message}><Input placeholder="RM 123456" {...form.register('professional_license')} /></FieldWrapper><FieldWrapper label="Correo" error={form.formState.errors.email?.message}><Input type="email" placeholder="profesional@clinica.com" {...form.register('email')} /></FieldWrapper><FieldWrapper label="Teléfono" error={form.formState.errors.phone?.message}><Input placeholder="+57 300 000 0000" {...form.register('phone')} /></FieldWrapper><FieldWrapper label="Especialidades" hint={specialties.isLoading ? 'Cargando especialidades…' : specialties.isError ? 'No se pudieron cargar las especialidades.' : specialties.data?.length === 0 ? 'No existen especialidades. Créalas en el módulo Especialidades.' : 'Busca y selecciona varias especialidades.'}><div className="space-y-2"><Input placeholder="Buscar por nombre" value={search} onChange={(e) => setSearch(e.target.value)} disabled={specialties.isLoading || specialties.isError} /><div className="flex flex-wrap gap-2">{selected.map((s) => <Chip key={s.specialty_id} label={s.specialty_name ?? 'Especialidad'} inactive={s.specialty_status === 'inactive'} onRemove={() => setSelected(selected.filter((x) => x.specialty_id !== s.specialty_id))} />)}</div>{search && selectable.length > 0 ? <div className="rounded-xl border border-slate-200 bg-white p-2 shadow-sm">{selectable.slice(0, 6).map((s) => <button key={s.id} type="button" className="block w-full rounded-lg px-3 py-2 text-left text-sm hover:bg-slate-50" onClick={() => { setSelected([...selected, { practitioner_id: editing?.id ?? '', specialty_id: s.id, specialty_name: s.name, specialty_status: s.status, status: 'active' }]); setSearch(''); }}>{s.name}</button>)}</div> : null}</div></FieldWrapper><div className="flex gap-2 md:col-span-2"><Button type="submit" disabled={save.isPending}>{save.isPending ? 'Guardando…' : 'Guardar cambios'}</Button>{editing ? <Button type="button" variant="secondary" onClick={closeForm}>Cancelar</Button> : null}</div></form></SectionCard> : null}<div className="mt-6"><SectionCard title="Listado de profesionales" description={`Tenant activo: ${tenant.label}`}>{practitioners.isLoading ? <LoadingState label="Cargando profesionales…" /> : null}{practitioners.isError ? <ErrorState description={(practitioners.error as Error).message} /> : null}{practitioners.isSuccess && practitioners.data.length === 0 ? <EmptyState title="Sin profesionales" description="Crea el primer profesional con datos básicos." /> : null}{practitioners.isSuccess && practitioners.data.length > 0 ? <DataTableShell columns={['Profesional', 'Tipo', 'Registro', 'Contacto', 'Estado', 'Acciones']} rows={practitioners.data.map((p) => [<NameEditButton name={p.full_name} onEdit={() => void startEdit(p)} />, p.professional_type ?? 'Sin tipo', p.professional_license ?? 'Sin registro', p.email ?? p.phone ?? 'Sin contacto', <StatusBadge status={p.status} />, <StatusAction status={p.status} entityName={p.full_name} disabled={statusAction.isPending} onInactivate={() => statusAction.mutate(p)} onActivate={() => statusAction.mutate(p)} />])} /> : null}</SectionCard></div></>; }
